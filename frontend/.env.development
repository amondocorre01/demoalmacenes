# Configuración del entorno de desarrollo local
# Si usas el backend en local (ej. corriendo en puerto 3001) o mediante el proxy de Vite
VITE_API_URL=https://sistemageneralbackend.capressocafe.com/
#http://localhost:3001/
